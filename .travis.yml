sudo: required

language: node_js
node_js:
- "10"

cache:
  yarn: true
  directories:
    - node_modules
    - app/node_modules

install:
  - yarn
  - travis_wait mvn install

script:
  - yarn lint
  - yarn test
  - yarn package-dev-linux
